import React from 'react';
import styled from 'styled-components';

const StyledWrapper = styled.div`
  font-family: 'Work Sans', sans-serif;
  margin-top: 100px;
  margin-bottom: 200px;
  margin-right: 800px;
`;

export const Contact = () => (
  <StyledWrapper>
    <p>are you trying to contact me for a busines inquiry or something else?</p>
  </StyledWrapper>
);
